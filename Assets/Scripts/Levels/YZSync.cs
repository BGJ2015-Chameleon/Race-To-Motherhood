using UnityEngine;
using System.Collections;

public class YZSync : MonoBehaviour {

	// Modifier for basic layering
	public float LayerOrder;
//	Transform	tr;
//	SpriteRenderer sr;
//	Camera camera;

	// Use this for initialization
	void Start () {
//		camera = Camera.main;
//		tr = GetComponent <Transform> ();
//		sr = GetComponent <SpriteRenderer> ();
//
//		Vector3 screenPos = camera.WorldToScreenPoint(tr.position);
//		Vector3 zLayer = new Vector3 (tr.position.x, tr.position.y, screenPos.y + LayerOrder);
//		float order = screenPos.y  + LayerOrder;
//		tr.position = zLayer;
//		sr.sortingOrder = (int) order;

	}
	
	// Update is called once per frame
	void Update () {
//		Vector3 screenPos = camera.WorldToScreenPoint(tr.position);
//		Vector3 zLayer = new Vector3 (tr.position.x, tr.position.y, screenPos.y + LayerOrder);
//		float order = screenPos.y  + LayerOrder;
//		tr.position = zLayer;
//		sr.sortingOrder = (int) order;
	}
}
